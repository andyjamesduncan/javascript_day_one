<html>
  <body>
    <p>
      Please enter a new name and artist?
    </p>

    <form action="/newsong" method="post">
      <input name="artist" />
      <input name="name" />
      <input type="submit" />
    </form>

    <% if @songlist.any? %>
      <ol>
        <% @songlist.each do |song| %>
          <li><%= song[0] + ' ' + song[1] %></li>
        <% end %>
      </ol>
    <% end %>
  </body>
</html>
